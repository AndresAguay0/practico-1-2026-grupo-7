{
  "name": "practica1-arrays-typescript",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Práctica 1 - TypeScript: tipos, arrays y callbacks",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.1.4"
  }
}
